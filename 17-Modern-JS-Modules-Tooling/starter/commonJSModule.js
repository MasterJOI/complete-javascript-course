/*
* Before implementation of native ES6 modules,
* there are other custom implementations like
* AMD modules, and CommonJS modules
* - CommonJS modules recently being used in node.js
* in 2019 Node.js 13.2.0 starts support ES6 modules
* */

// CommonJS export

/*export.addToCard = function(product, quantity) {
  card.push({product, quantity});
  console.log(`${quantity} ${product} added to cart`);
};*/

// CommonJS import

//const {addToCard} = require('./commonJSModule.js');
